# apifuzz

API Security Testing Framework — OWASP API Top 10 test generation and execution.

## Install

```bash
pip install apifuzz
```

## Quickstart

```bash
apifuzz scan openapi.yaml --target https://api.example.com --output sarif --out-file results.sarif
```
